<%= form_with url: drinks_url do |form| %>

  <% if @drink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drink.errors.count, "error") %> prohibited this consumed_drink from being saved:</h2>

      <ul>
      <% @drink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div data-controller="drinks">
    <div class="control">
      <label class="label is-large">What are you drinking?</label>
      <div class="select is-large">
        <select>
          <option>Select Drink Type</option>
          <% @categories.each do |cat| %>
            <option><%= cat.name %></option>
          <% end %>
        </select>
      </div>
    </div>

    <div data-target="drinks.newDrinkContainer">

    </div>
  </div>

  
<% end %>